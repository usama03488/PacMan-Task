using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameWall : Matrix
{
    // Start is called before the first frame update
    void Start()
    {
        setArray();
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
  
}
